<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">

  <Import Project="..\..\DnSpyCommon.props" />

  <PropertyGroup>
    <AssemblyName>dnSpy.Extension.MCP.x</AssemblyName>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\dnSpy\dnSpy.Contracts.DnSpy\dnSpy.Contracts.DnSpy.csproj" />
    <ProjectReference Include="..\..\dnSpy\dnSpy.Contracts.Logic\dnSpy.Contracts.Logic.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.VisualStudio.Text.UI.Wpf" Version="$(MSVSTextVersion)" />
  </ItemGroup>

  <!-- ASP.NET Core packages - only for .NET (not .NET Framework 4.8) -->
  <ItemGroup Condition=" '$(IsDotNet)' == 'true' ">
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <!-- For .NET Framework 4.8, we'll use a simpler HTTP listener approach -->
  <ItemGroup Condition=" '$(IsDotNetFramework)' == 'true' ">
    <PackageReference Include="Newtonsoft.Json" Version="$(NewtonsoftJsonVersion)" />
  </ItemGroup>

  <!-- System.Text.Json for both targets -->
  <ItemGroup>
    <PackageReference Include="System.Text.Json" Version="8.0.5" Condition=" '$(IsDotNet)' == 'true' " />
    <PackageReference Include="System.Text.Json" Version="8.0.5" Condition=" '$(IsDotNetFramework)' == 'true' " />
  </ItemGroup>

</Project>
